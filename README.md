# LaneLandingPage

Static GitHub Pages site for Lane Birmingham.

## Local preview

Open `index.html` directly or run a simple static server, for example:

```bash
python3 -m http.server
```

The GitHub Actions workflow in `.github/workflows/deploy-pages.yml` publishes the site to GitHub Pages on pushes to `main`.
